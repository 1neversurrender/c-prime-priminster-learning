//
//#define _CRT_SECURE_NO_WARNINGS
//#include<stdio.h>
//#define min1 60
//int main(void)
//{
//	int input = 1;
//	while (input > 0)
//	{
//		printf("please enter the total number of min");
//		scanf("%d" , &input);
//	}
//}
//#include<stdio.h>  
//int main(void)
//{
//    int input;
//    while (1) // 使用无限循环，直到用户输入负数  
//    {
//        printf("Please enter the total number of minutes (or a negative number to quit): ");
//        if (scanf("%d", &input) == 1 && input > 0) {
//            // 如果成功读取一个正整数，则执行循环体（这里只是继续循环）  
//            // 实际上，这个循环体是空的，因为只要输入是正数就继续循环  
//        }
//        else {
//            // 如果输入不是整数或小于等于0，则退出循环  
//            printf("Invalid input. Please enter a positive integer.\n");
//            // 清除输入缓冲区中的剩余字符（可选，但有助于处理错误输入）  
//            while (getchar() != '\n' && getchar() != EOF);
//            if (input <= 0) break; // 如果输入小于等于0，则退出循环  
//        }
//    }
//    printf("Exiting program.\n");
//    return 0;
//}
